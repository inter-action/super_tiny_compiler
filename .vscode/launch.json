{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceRoot}/target/debug/super_tiny_compiler",
            "args": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}
